export function _M_Array(length) {
    let arr_obj = {};
    this.length = () => length;
    
    this.getElem = (idx) => { // element getter function

    }

    this.setElem = (idx,value) => {

    }

    this.search = (val,from=0,to=length) => {
        
    }

    this.sort = (val,from=0,to=length) => {

    }

}